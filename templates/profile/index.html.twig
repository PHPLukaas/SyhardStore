{% extends 'base.html.twig' %}

{% block title %}Hello ProfileController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="container">
        <h1 class="text-center">{{ compte }}</h1>

        <div class="card mb-4 bg-secondary text-dark">
            <div class="card-body">
                <p class="card-text">{{ firstname }} {{ lastname }}</p>
                <p class="card-text">{{ email }}</p>

                <a href="{{ path('app_logout') }}" class="rounded nav-link d-flex text-decoration-underline">
                    <p class="">Déconnexion</p>
                </a>
            </div>
        </div>
        <div class="card mb-4 bg-secondary text-dark">
            <div class="card-title">
                <div class="card-body">
                    <h3 class="card-title">CARNET D'ADRESSES</h3>
                    <p class="card-text">{{ firstname }} {{ lastname }}</p>
                    <p class="card-text">{{ adresse }}</p>
                    <p class="card-text">{{ code_postal }} {{ ville }}</p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
